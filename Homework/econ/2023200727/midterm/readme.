In kaggle, my name is uzawarisa.
在我们的数据清洗函数体系中，我们严格区分两种类型的函数来处理测试集泄露风险：

Type1 函数（无泄露风险）

    示例：first_numeric_clean - 使用正则表达式提取数字

    特征：基于确定性规则，不涉及数据分布学习

    原理：可安全应用于训练集和测试集，无信息泄露

Type2 函数（需防泄露设计）

    示例：to_dummy_multiple - 创建哑变量

    关键保护机制：

        仅从训练集提取类别标签

        测试集中独有的类别被标记为全0

        确保转换规则完全源于训练数据分布

这种设计确保了我们的特征工程流程：

    训练阶段：仅基于训练集学习转换规则

    测试阶段：应用训练阶段学到的规则，避免任何测试集信息泄露

通过这种精细的type1/type2分类，我们在保持代码简洁性的同时，从根本上杜绝了测试集污染问题，确保了模型评估的真实性和可靠性。
